<div class="container mx-auto">
  <div class="page-body">
    <div class="container-xl">
      <h1 class="text-center text-white">Forms</h1>
      <div class="row row-deck row-cards">
        <div class="col-12">
          <div class="card">
            <div class="card-body">
              <table class="table">
                <thead>
                  <tr>
                    <th>Title</th>
                    <th>Description</th>
                  </tr>
                </thead>
                <tbody>
                  <% @forms.each do |form| %>
                    <tr>
                      <td><%= link_to form.title, feedbacks_path(feedback: { form_id: form.id }) %></td>
                      <td><%= form.description %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
              <% if @forms.blank? %>
                <p>No forms could be found.</p>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
